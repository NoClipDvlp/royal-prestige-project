import { AdminPageSkeleton } from "@/components/skeletons";

// Placeholder instantáneo al navegar a /admin (título + tabs + managers).
export default function AdminLoading() {
  return <AdminPageSkeleton />;
}
