import { TareasPageSkeleton } from "@/components/skeletons";

// Placeholder instantáneo al navegar a /tareas (título + day-nav + timeline).
export default function TareasLoading() {
  return <TareasPageSkeleton />;
}
