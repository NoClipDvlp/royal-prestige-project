import { MetricsPageSkeleton } from "@/components/skeletons";

// Placeholder instantáneo al navegar a /metricas (neutral: aún no se conoce el rol).
export default function MetricasLoading() {
  return <MetricsPageSkeleton />;
}
