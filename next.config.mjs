/**
 * Royal Control — Next.js config (Hito 0).
 *
 * ⚠ ARCHIVO CORE (.ai/core/.coreignore → next.config.mjs).
 * No editar por iniciativa. Cambios requieren ADR + [CORE-APPROVED: ADR-XXXX].
 *
 * Hito 0: configuración vacía a propósito. El cableado de PWA, headers de
 * seguridad e imágenes remotas se añadirá en hitos posteriores (con su ADR).
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {};

export default nextConfig;
